
name: CI
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        run: echo Hello, world!
  test:
      runs-on: ubuntu-latest
      needs: build 
      
      steps:
        - name: Run a one-line script
          run: echo test done!


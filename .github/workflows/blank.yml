name: HTML Action Demo
on: push
jobs:
  html-demo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code
        uses: actions/checkout@v4
      - name: print index.html file  
        run: cat index.html
